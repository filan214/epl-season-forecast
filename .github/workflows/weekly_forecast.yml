# Weekly forecast pipeline — IMPLEMENTATION_GUIDE.md §5.
#
# PLACEHOLDER (built in Phase 6). The real workflow will: restore the parquet
# cache, fetch new results + xG, rebuild features with cutoff=now, fit PyMC and
# run the convergence gate, blend, simulate the season (both uncertainty
# variants), and write predictions for all remaining fixtures BEFORE any kickoff.
#
# Manual-trigger only for now, so nothing runs on a schedule before it exists.
name: weekly-forecast

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "weekly_forecast is scaffolded but not yet implemented (IMPLEMENTATION_GUIDE.md §5, Phase 6)."
